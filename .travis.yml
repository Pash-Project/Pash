language: csharp

sudo: false   # use the new container-based Travis infrastructure

mono:
  - latest
  - 3.2.8

# use Travis Containers - http://docs.travis-ci.com/user/workers/container-based-infrastructure/
sudo: false

notifications:
  email:
    on_success: always

script: "xbuild /target:test"
